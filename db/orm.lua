return {
    tariff = require("db.entities.tariff"),
    client = require("db.entities.client"),
    token = require("db.entities.token"),
    balance = require("db.entities.balance"),
    operation = require("db.entities.operation"),
    operation_type = require("db.entities.operation_type"),
    client_resources = require("db.entities.client_resources"),
    tariff_resources = require("db.entities.tariff_resources")
}

